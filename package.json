{
  "name": "taskly-api",
  "version": "1.0.0",
  "description": "A minimal task management REST API — demo repo for karancode/kiro-action",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "jest --forceExit",
    "lint": "eslint src/ tests/"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "jest": "^29.7.0",
    "supertest": "^6.3.4"
  },
  "jest": {
    "testMatch": ["**/tests/**/*.test.js"]
  }
}
